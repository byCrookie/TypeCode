<UserControl x:Class="TypeCode.Wpf.Pages.Common.Configuration.ConfigurationWizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:configuration="clr-namespace:TypeCode.Wpf.Pages.Common.Configuration"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance configuration:ConfigurationWizardViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5">
            <Button Margin="5,0,5,0" BorderThickness="0" Command="{Binding FormatCommand}" Content="Format" />
            <Button Margin="0,0,5,0" BorderThickness="0" Command="{Binding ReloadCommand}" Content="Reload" />
            <Button BorderThickness="0" Command="{Binding SaveCommand}" Content="Save" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <TextBox
                BorderThickness="0"
                Padding="2" Text="{Binding Configuration, UpdateSourceTrigger=PropertyChanged}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                AcceptsTab="True" />
        </ScrollViewer>
    </Grid>
</UserControl>